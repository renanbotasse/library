{
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install",
  "buildCommand": "pnpm turbo build --filter=@tomekeep/web...",
  "outputDirectory": "apps/web/.next"
}
